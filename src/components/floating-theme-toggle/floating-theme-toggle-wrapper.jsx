import React, { useEffect, useRef } from 'react';
import Fab from '@mui/material/Fab';
import Brightness7Icon from '@mui/icons-material/Brightness7';


const FloatingThemeToggleWrapper = ({ element }) => {

  const fabStyle = {
    position: 'absolute',
    bottom: 20,
    left: 16,
  };

  return (
    <>
      
    </>
  );
};

export default FloatingThemeToggleWrapper;
