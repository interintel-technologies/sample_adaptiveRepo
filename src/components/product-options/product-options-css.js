import { css } from "lit-element";

export const ProductOptionsStyles = css`
    li {
        border-top: 0.5px solid var(--grey-dark);
    }
    li:last-child {
        border-bottom: 0.5px solid var(--grey-dark);
    }
`;
