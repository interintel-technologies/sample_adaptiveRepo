import { css } from "lit-element";

export const InsightPanel1Styles = css `
.profile-initial{
    color: #fff;
    width: 48px;
    height: 48px;
    display: flex;
    border-radius: 50%;
    justify-content: center;
    background-color: var(--primary);
}
.profile-initial > p{
    top: 24%;
    font-weight: bold;
    position: relative;
}
`