
import {css} from 'lit-element';

export const DiscountedProductStyles = css`
    .columns{
        padding:16px;
    }
    .card {
        border: 2px solid var(--primary);
    }
    .card:hover {
        border-color: var(--link);
    }
`;
